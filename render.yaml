services:
  - type: web
    name: emee-z-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: EVOLUTION_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
